# Acceptance Matrix (v1)

This test matrix maps controlled tests to criteria 1–20 from the role contract (`docs/outside_orchestrator_role_v2.md` §10) and is automated via [`criteria.test.ts`](file:///c:/Users/Michael/Outside_Orchestrator/Outside_Orchestrator/tests/acceptance/criteria.test.ts).

- [x] 1. Duplicate idempotency key cannot create second run (`AC 1: Duplicate idempotency key cannot create second authoritative run`)
- [x] 2. Lease/fence guarded durable transition, restart-resumable (`AC 2: Lease/fence guarded durable transition, restart-resumable`)
- [x] 3. Sandbox receives only short-lived scoped credentials (`AC 3: Sandbox receives only short-lived scoped credentials`)
- [x] 4. Expected Tailscale ephemeral identity, denied management ports (`AC 4: Expected Tailscale ephemeral identity, denied management ports`)
- [x] 5. False changed-files claim rejected by host tree reconciliation (`AC 5: False changed-files claim rejected by host tree reconciliation`)
- [x] 6. Frozen suite cannot be replaced by builder (`AC 6: Frozen suite cannot be replaced or tampered with by builder`)
- [x] 7. Stale/duplicate/out-of-order callbacks cannot advance state (`AC 7: Stale/duplicate/out-of-order callbacks cannot advance state`)
- [x] 8. Tier 3 runtime access remains tenant/run scoped (`AC 8: Tier 3 runtime access remains tenant/run scoped`)
- [x] 9. Tournament arms remain isolated (`AC 9: Tournament arms remain isolated and cannot overwrite each other`)
- [x] 10. Harvest requires verified signature + tree SHA (`AC 10: Harvest requires verified signature + tree SHA`)
- [x] 11. Teardown revokes access + destroys VM across terminal flows (`AC 11: Teardown revokes access + destroys VM across terminal flows`)
- [x] 12. DR rehearsal meets declared RTO (`AC 12: DR rehearsal meets declared RTO`)
- [x] 13. Tailscale policy validate/apply honors ETag concurrency (`AC 13: Tailscale policy validate/apply honors ETag concurrency`)
- [x] 14. CI/CD identities remain separated by operation (`AC 14: CI/CD identities remain separated by operation`)
- [x] 15. Sandbox auth key is ephemeral/single-use scoped (`AC 15: Sandbox auth key is ephemeral/single-use scoped`)
- [x] 16. Continuous signed evidence chain exists (`AC 16: Continuous signed evidence chain exists and detects tampering`)
- [x] 17. CLEAN_TERMINATED impossible without full proof set (`AC 17: CLEAN_TERMINATED impossible without full proof set`)
- [x] 18. Advisory output must be collected + hash verified (`AC 18: Advisory output must be collected + hash verified`)
- [x] 19. Sandbox cannot renew lease; silent sandbox externally terminated (`AC 19: Sandbox cannot renew lease; silent sandbox externally terminated by watchdog`)
- [x] 20. One-phase-per-attempt, isolated-only delegation, fresh VM default (`AC 20: One-phase-per-attempt, isolated-only delegation, fresh VM default`)
