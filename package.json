{
  "name": "outside_orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "check": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.6.2"
  }
}
